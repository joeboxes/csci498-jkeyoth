/**
 * The Main class initializes a new Maze game and starts it.
 */
class Main {

    /** Initializes a new game and starts it. */    
    function void main() {
        var MazeGame game;

        let game = MazeGame.new();
        do game.run();
	do game.dispose();

        return;
    }
}
